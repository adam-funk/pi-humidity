TODO

Client:

On each pass, write to sqlite DB.
Columns:
epoch, iso_time, identifier, temperature, humidity, transmitted=false

Then query DB for transmitted=false; try to transmit each one and
update DB.

*****
Server config:
 - data file
 - log file
 - server port

Run server and plot-mailer on another account with $HOME available at boot.

crontab @reboot sensor-server.py start

cron.daily or similar after texpire (oom-killer)
  restart

*****
Plotter:
- separate by identifier

*****
DONE
Use bottledaemon; start from cron @reboot.
